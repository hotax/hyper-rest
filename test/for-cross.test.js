const proxyquire = require('proxyquire'),
    path = require('path');

describe('Cross', () => {
    let stubs
    beforeEach(() => {
        stubs = {};
        err = new Error('any error message');
    })

    describe('注册资源', () => {
        it('使用缺省', () => {
        })
    })

    describe('上载服务', () => {
        // TODO: 测试上载服务
    })
})